# regularly spaced grid of numbers

# Find a way to generate a list of numbers from `start=-10` to
# `stop=10` (exclusive) with `step=0.25`, i.e., `[-10., -9.75, -9.5,
# ..., 9.5, 9.75]`.
#
# Store your list in the variable `x_grid`.
